[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    torch: needs torch (CPU is enough)
    model: needs the full model environment (pytorch3d, manotorch, MANO assets)
    realdata: needs a checkpoint and the released example data
    slow: renders video or runs the network
